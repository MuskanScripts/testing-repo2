import { useEffect } from 'react';

export default function Home() {
  useEffect(() => {
    if (typeof document === 'undefined') return;
    // No specific home page logic in provided file
  }, []);
  return null;
}
